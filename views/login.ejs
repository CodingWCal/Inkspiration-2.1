<!doctype html>
<html data-theme="inkspiration">
<head>
  <title>InkSpiration Login</title>
  <link rel="stylesheet" href="/styles/output.css">
</head>
<body class="min-h-screen flex items-center justify-center px-4 bg-gradient-to-br from-[#E5D8FA] via-[#C2E8E4] to-[#F3F4F6] [cursor:url('/img/cursor.cur'),_auto]">

  <div class="card w-full max-w-md bg-base-100 shadow-xl p-8 space-y-6 text-center border border-base-300">

    <h1 class="text-3xl font-bold text-primary border-b border-base-300 animate-fade-down">Login to Create ✏️</h1>

    <% if (message.length > 0) { %>
      <div class="alert alert-error text-sm"><%= message %></div>
    <% } %>

    <!-- LOGIN FORM -->
    <form action="/login" method="post" class="space-y-4 animate-fade-up delay-100">
      <div class="form-control">
        <label class="label">
          <span class="label-text text-base-content">Email:</span>
        </label>
        <input 
        type="text" 
        name="email" 
        placeholder="you@example.com"
        class="input input-bordered w-full bg-white text-black border border-primary rounded-md"
        required>      
      </div>

      <div class="form-control">
        <label class="label">
          <span class="label-text text-base-content">Password:</span>
        </label>
        <input 
        type="password" 
        name="password" 
        placeholder="********"
        class="input input-bordered w-full bg-white text-black border border-primary rounded-md"
        required>      
      </div>

      <button type="submit" class="btn border-2 border-[#91dbd1] w-full text-[#7B2CBF] font-bold hover:bg-[#91dbd1] hover:text-white hover:font-bold px-4 py-2 rounded-md shadow-md hover:scale-105 transition-all duration-300">Login</button>
    </form>

    <div class="divider text-base-content">or</div>

    <p class="text-sm text-base-content animate-fade-down delay-150">
      Need an account? <a href="/signup" class="link link-accent font-bold border-b border-base-700">Sign up</a><br>
      Or go <a href="/" class="link link-primary font-bold border-b border-base-700">home</a>.
    </p>
  </div>

</body>
</html>
